# 校园二手交易小程序测试说明与修改提交清单

测试日期：2026-06-24  
测试对象：`feature/migrate-frontend-auth` 分支当前本地代码  
测试方式：对照测试报告、需求文档与当前前后端代码进行静态复核；部分问题结合微信开发者工具实际现象判断。

## 1. 测试依据

本次检查参考了以下测试与需求文档：

- `TESTING_REPORT.md`
- `TESTING_REPORT_复测(1).md`
- `TESTING_REPORT_租赁_求购.md`
- `问题.md`
- `docs/SRS-CAU校园二手交易小程序.md`
- `docs/SDD-CAU校园二手交易小程序概要设计说明书.md`
- `docs/DDD-CAU校园二手交易小程序数据库设计说明书.md`
- `docs/SDDD-CAU校园二手交易小程序详细设计说明书(1).docx`

## 2. 本次分支已融合或修复的内容

### 2.1 L2 学生认证流程

当前分支已补充 L2 认证前后端逻辑：

- 用户端可提交真实姓名、学号、学生证/证件照片。
- 后端新增学生认证申请查询与提交接口。
- 管理员后台新增 L2 认证审核入口，可查看待审核申请并通过或驳回。
- 审核通过后用户认证等级可升级为 L2。
- 管理员审核列表中已返回用户邮箱，避免“明明 L1 已绑定邮箱但审核卡片显示未绑定邮箱”的问题。
- 前端对“已有审核中的申请”进行了兜底处理，避免提交后因返回值为空导致 `status of undefined` 报错。

### 2.2 注册与密码页面迁移

当前分支已将“注册与改密”页面按认证流程重新整理：

- 邮箱认证、设置登录密码、重置登录密码、设置安全密码、重置安全密码、L2 认证在同一页面中分 tab 处理。
- 忘记密码入口不再要求用户先登录。
- 设置安全密码与重置安全密码区分开，解决“安全密码设置后无法修改”的问题。

### 2.3 管理后台相关调整

当前分支已补充管理员后台部分功能：

- 用户管理中展示用户邮箱、认证等级、信用分、封禁状态。
- 认证审核 tab 可查看 L2 申请。
- 举报处理、信用分调整、用户封禁等后台功能已有页面入口。

## 3. 已确认修复的历史问题

| 编号 | 问题 | 当前结论 |
|---|---|---|
| B1 | 用户资料接口存在 IDOR 越权风险 | 已修复，资料接口改为 `/api/v1/users/me`，用户 ID 从 token 获取 |
| B2/F4 | 买家下单后拿不到取货码，订单页不展示取货码 | 已修复，交易返回 `secureToken`，前端订单页读取展示 |
| B4 | 卖家核销取货码后商品没有变为 Sold | 已修复，核销成功后商品状态会同步为 Sold |
| L2-1 | L2 认证缺少管理员审核闭环 | 已补充用户提交与管理员审核流程 |
| L2-2 | L2 提交后出现 `status of undefined` | 已修复，前端对返回值做了兜底 |
| P1 | 安全密码无法重置 | 已补充重置安全密码入口与后端接口 |

## 4. 当前仍需修改的问题

以下问题在当前代码中仍未完全修复，建议作为后续提交重点。

| 优先级 | 编号 | 问题 | 建议修改点 |
|---|---|---|---|
| 高 | R1 | 图片上传接口返回相对路径 `/uploads/xxx`，前端直接保存，可能导致小程序图片无法显示 | 后端返回完整 URL，或前端在 `publish.js` 中用 `baseURL` 拼接 |
| 高 | F3 | “我的订单”页联系按钮使用 `wx.navigateTo` 跳转 Tab 页，点击无效 | 改为保存聊天参数后 `wx.switchTab({ url: '/pages/chat/chat' })` |
| 高 | B3 | 封禁只影响重新登录，旧 token 仍可继续调用接口 | 增加认证中间件或授权校验，每次请求检查 `IsBanned`；微信登录也要检查封禁状态 |
| 中 | B6/F2 | 商品列表缺少成色筛选 | 后端 `ItemQuery` 增加 `ConditionLevel`，前端筛选面板增加成色 picker |
| 中 | B7 | 管理员采纳举报不自动扣信用分 | `HandleReport()` 中根据举报原因扣分，写入 `CreditLog`，必要时封禁 |
| 中 | F1 | 首页分类第二次点击商品 Tab 不刷新 | 商品页 `onShow` 中读取 `goodsFilterCategory` 并重新拉取列表 |
| 中 | B5 | 租赁功能只做了部分后端字段，用户无法发布租赁商品 | 发布/编辑页增加租赁开关、租金、押金；编辑接口传递租赁字段 |
| 中 | M3 | 求购功能有后端和 API 封装，但没有前端页面入口 | 新增求购大厅页面、发布求购页面，并在首页或 Tab 中提供入口 |

## 5. 与需求文档的主要差距

当前项目已具备基础二手交易 MVP，但尚未完全满足 `docs` 中的完整需求。

### 5.1 认证与权限

- SRS 中定义 L2 用户拥有完整交易权益，但当前多数接口只要求 L1。
- 安全密码已经可以设置和重置，但交易确认等敏感操作尚未强制校验安全密码。
- 封禁用户的旧 token 尚未失效。

### 5.2 商品与租赁

- 商品发布、浏览、搜索、分类、成色展示已具备。
- 成色筛选、学院标签、免费赠送快捷模式、图片水印尚未实现。
- 租赁商品详情可展示租赁字段，但发布页没有租赁入口，租赁闭环不完整。

### 5.3 求购模块

- 后端已有求购接口：发帖、列表、响应、续期、删除。
- 前端没有求购大厅页面，也没有求购入口，用户无法实际使用。

### 5.4 交易与风控

- 普通交易取货码闭环基本可用。
- 取货码当前仍以明文形式存储在交易表中，不符合 DDD 中“仅保存 HMAC 哈希”的要求。
- 举报、信用分、封禁之间没有自动联动。
- 黑名单已有接口，但尚未阻止被拉黑用户查看商品或发起聊天。

### 5.5 数据统计与报表

- 管理后台已有简单仪表盘。
- 需求文档中的日/周/月统计、自定义时间筛选、搜索词统计、校区/学院维度统计、报表导出、每日 ETL 聚合表尚未实现。

## 6. 验证限制

本次检查尝试执行后端编译：

```powershell
dotnet build "src\CAUSecondHand.slnx" --no-restore
```

但当前环境没有安装可用的 .NET SDK，命令返回 `No .NET SDKs were found`。因此本报告只能说明静态代码检查与页面现象判断结果，不能证明后端已编译通过。

## 7. 建议提交说明

本次分支建议提交内容可概括为：

> 迁移并完善前端认证相关页面，补充 L2 学生认证申请与管理员审核流程，修复安全密码不可重置、L2 审核邮箱显示异常、L2 重复提交前端报错等问题。

提交前建议重点确认：

- `.env` 不要提交。
- `project.private.config.json` 如为本机微信开发者工具配置，建议不要提交。
- 如本次只提交认证迁移相关改动，图片 URL、订单联系按钮、成色筛选、求购大厅、租赁发布入口等问题应在后续分支继续修复。

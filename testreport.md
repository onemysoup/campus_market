# 校园二手交易小程序复测说明与当前问题清单

测试日期：2026-06-24；2026-06-25 补充复测

测试对象：`fix/testreport-issues` 分支当前本地代码

测试方式：对照测试报告、`docs/` 需求与设计文档、`WORK_SUMMARY_feature-gap-fixes.md` 进行代码复核；结合微信开发者工具现象进行问题定位；后端使用 Docker 构建与启动验证。

## 1. 测试依据

本次检查参考以下文档与测试记录：

- `TESTING_REPORT.md`
- `TESTING_REPORT_复测(1).md`
- `TESTING_REPORT_租赁_求购.md`
- `问题.md`
- `WORK_SUMMARY_fix-testreport-issues.md`
- `WORK_SUMMARY_feature-gap-fixes.md`
- `docs/SRS-CAU校园二手交易小程序.md`
- `docs/SDD-CAU校园二手交易小程序概要设计说明书.md`
- `docs/DDD-CAU校园二手交易小程序数据库设计说明书.md`
- `docs/SDDD-CAU校园二手交易小程序详细设计说明书(1).docx`

## 2. 本轮已修复 / 已融合的问题

### 2.1 小程序运行与显示问题

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| F-LOAD | 微信开发者工具报 `Identifier 'upload' has already been declared`，页面空白 | 已修复 | `miniprogram/api/files.js` 改为通过 `requestUtil.upload(...)` 调用，避免重复声明 |
| F-TIME | 新发布商品一直显示 `8小时前` | 已修复 | `formatTime()` 兼容后端返回的无时区 UTC 时间，避免按北京时间误解析 |
| R1 | 图片上传返回相对路径导致图片可能无法显示 | 已修复 | 上传接口 / 前端链路已能正常显示上传图片 |

### 2.2 认证、登录与权限

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| L2-1 | L2 学生认证缺少管理员审核闭环 | 已修复 | 用户可提交姓名、学号、证件照片；管理员可审核通过/驳回 |
| L2-2 | L2 提交审核后出现 `status of undefined` | 已修复 | 前端对返回结果做兜底，已有审核中申请会正常提示 |
| L2-3 | 管理端 L2 审核卡片显示邮箱未绑定 | 已修复 | 管理端审核列表可显示用户邮箱 |
| P1 | 安全密码设置后无法重置 | 已修复 | 注册与改密页新增重置安全密码入口和接口 |
| B3 | 封禁只影响重新登录，旧 token 仍可继续请求 | 已修复 | JWT 校验阶段检查用户是否已封禁；微信登录路径也检查封禁状态 |
| AUTH-1 | 忘记密码、注册账号误提示“请先登录” | 已修复 | 登录页入口可直接进入注册 / 重置流程 |

### 2.3 商品、租赁与发布

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| B6/F2 | 商品列表缺少成色筛选 | 已修复 | 后端 `ItemQuery` 支持 `ConditionLevel`，前端筛选面板已提供成色选项 |
| F1 | 首页分类第二次点击商品 Tab 不刷新 | 已修复 | 商品页 `onShow` 会读取分类筛选并刷新 |
| B5 | 发布页缺少租赁入口，编辑租赁字段不完整 | 已修复 | 发布 / 编辑页支持租赁开关、租金、押金；从租赁切换为非租赁会清空租赁字段 |
| ITEM-1 | L1 用户发布高价商品权限过宽 | 已修复 | L1 仅可发布 200 元以下商品；编辑商品时也校验目标价格 |
| ITEM-2 | 免费赠送快捷模式缺失 | 已修复 | 发布页新增“0元赠送”开关，开启后价格固定为 0 |
| ITEM-3 | 发布商品成功后草稿 / 页面表单未清空，下次进入仍残留上次内容 | 已修复 | 发布成功后同时清除 `publishDraft_<userId>` 本地草稿和 tabBar 复用页面的内存表单；发布页所有字段变更都会同步草稿 |
| ITEM-4 | 发布时关闭“可议价”，发布后详情仍显示“可议价” | 已修复 | 后端 `Item` 实体、发布 DTO、编辑 DTO、控制器均补齐 `IsNegotiable` 写入；免费 / 租赁商品强制不可议价 |
| ITEM-5 | 租赁商品在部分列表仍显示 `¥0.00` | 已修复 | 浏览历史、我的订单、管理端商品列表、聊天列表、聊天详情商品条均改为显示租金；免费商品显示“免费” |

### 2.4 交易与风控

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| B2/F4 | 买家下单后拿不到取货码，订单页不展示取货码 | 已修复 | 创建交易返回取货码，订单页可展示 |
| B4 | 卖家核销取货码后商品没有变为 Sold | 已修复 | 核销成功后交易和商品状态会同步更新 |
| TX-1 | 取货码明文存储 | 已修复 | 新交易保存 HMAC 哈希；旧明文记录保留兼容校验与展示 |
| TX-2 | 安全密码不应强制用于购买 / 取消 / 核销，但应可由用户自行开启 | 已修复 | 后端把安全密码改为可选二次确认；前端在账号设置中新增“安全密码使用设置”，可分别开启购买商品、取消订单、核销取货码 |
| TX-3 | L2 完整交易权益未落实 | 已修复 | 购买商品要求 L2 用户 |
| F3 | “我的订单”联系按钮跳转 Tab 页无效 | 已修复 | 改为保存聊天参数后 `wx.switchTab()` 到消息页 |
| TX-4 | 购买成功后前端提示“服务器繁忙”，但订单已创建 | 已修复 | 购买成功通知改为尽力发送，订阅消息失败不会回滚或影响下单响应 |
| TX-5 | 我的订单状态 Tab 不筛选列表 | 已修复 | 前端传递 `status` 查询参数，后端按交易状态过滤 |
| TX-6 | 我的订单买家 / 卖家显示未知用户 | 已修复 | 交易列表 DTO 增加买家、卖家昵称，前端按当前角色显示对方名称 |

### 2.5 求购模块

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| M3 | 求购模块有后端但没有前端入口 | 已修复 | 新增求购大厅、发布、响应、续期、关闭等前端流程 |
| REQ-1 | “我有它”只刷计数，没有真实响应闭环 | 已修复 | 响应时必须选择本人在售商品，发布者可查看响应商品 |
| REQ-2 | 求购响应可重复刷计数 | 已修复 | 服务端按 `(求购帖, 卖家, 商品)` 幂等去重 |
| REQ-3 | 求购重复发布检测缺失 | 已修复 | 24 小时内同一用户发布高度相似标题会被拒绝 |

### 2.6 管理后台、举报与黑名单

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| B7 | 管理员采纳举报不自动扣信用分 | 已修复 | 举报处理会写入信用记录并扣分 |
| REP-1 | 重复调用举报处理接口会重复扣分 | 已修复 | 已处理举报不可重复处理 |
| BLK-1 | 黑名单只保存关系，没有真正拦截 | 已修复 | 商品列表/详情、聊天会话、消息读取与发送均增加双向黑名单拦截 |
| EF-1 | API 启动提示 EF model pending changes | 已修复 | 补齐 `RequestResponse` 在迁移 designer 与 `AppDbContextModelSnapshot` 中的模型快照 |

### 2.7 数据统计与报表

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| STATS-1 | 数据统计报表只有简单仪表盘，缺少日/周/月/自定义区间 | 已修复 | `/api/v1/admin/stats/reports` 支持 `day/week/month/custom` 和校区筛选 |
| STATS-2 | 管理后台没有多维运营报表展示 | 已修复 | 管理员仪表盘新增运营报表区，展示新增用户、发布商品、完成交易、成交率、成交金额、活跃用户、校区对比、分类表现、热门商品 |
| STATS-3 | 报表不支持导出 | 已修复 | 新增 `/api/v1/admin/stats/export` CSV 导出；小程序端支持复制 CSV |
| STATS-4 | 每日 ETL 没有持久化落表 | 已修复 | 新增 `t_stats_daily`，`DailyEtlJob` 每日按东/西校区写入预聚合统计 |
| STATS-5 | 页面点击量、搜索关键词云统计缺失 | 已修复 | 新增 `EventLog` / `SearchLog` 埋点，管理员报表展示页面点击与搜索热词，ETL 会汇总入日报 |

### 2.8 FEATURE_GAP 补充功能

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| FG-1 | 商品图片自动合成 CAU 水印 | 已修复 | 图片上传接口对 jpg / jpeg / png / webp 合成“CAU 二手交易专用”半透明水印；gif 原样保存以避免破坏动图 |
| FG-2 | AI 辅助生成商品描述缺失 | 已修复 | 发布页接入 AI 描述接口；后端支持外部 OpenAI 兼容接口，未配置或调用失败时自动降级为本地规则生成 |
| FG-3 | 学院标签筛选缺失 | 已修复 | 商品 / 求购支持关联学院，商品列表与求购列表支持学院筛选，管理员报表展示学院维度统计 |

### 2.9 微信订阅通知与求购匹配

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| WX-1 | 求购响应后不通知求购发布者 | 已接入，待微信模板联调 | 响应成功后后端会调用微信订阅消息接口；未配置模板 ID 时自动跳过，不影响本地测试 |
| WX-2 | 新商品发布后不自动匹配求购 | 已接入，待微信模板联调 | 商品发布成功后会按校区、价格、资源类型和标题相似度匹配有效求购，并向买家发送订阅消息 |
| WX-3 | 小程序端没有请求订阅授权 | 已接入，待微信模板联调 | 发布求购前会请求“有人响应”和“新商品匹配”订阅授权；模板 ID 留空时自动跳过 |
| WX-4 | 每次购买都会重复弹出订阅通知授权 | 已修复，待微信模板联调 | 小程序本地记录用户对购买成功通知的允许 / 拒绝选择，同一用户同一模板不再反复拉起授权框 |
| WX-5 | 购买成功通知模板未接入 | 已接入，待微信模板联调 | 前端与后端均预留购买成功模板 ID 配置，后端下单成功后向买家发送购买成功订阅消息 |

### 2.10 本轮新增修复项（2026-06-25）

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| MY-1 | 新微信注册 L0 用户进入“我的”页提示无权限，无法去做邮箱认证 | 已修复 | L0 用户进入“我的”页时不再调用 L1 信用接口；账号设置保留“绑定邮箱认证”入口 |
| L2-4 | 已认证学号可被另一个用户再次提交审核 | 已修复 | L2 提交与管理员审核通过前均校验学号是否已被其他用户使用或已有待审 / 已通过申请 |
| CHAT-1 | 第一条对方消息误显示在右侧 | 已修复 | 根因是首条消息内的时间分割线与气泡处于同一 flex 行，导致对方气泡被挤到右侧；已改为纵向布局并保留消息归属判断 |
| CHAT-2 | 从我的订单联系卖家可能新开会话，看不到历史聊天记录 | 已修复 | 消息页进入聊天详情前会按对方与商品查找已有会话并复用 `sessionId` |
| SEC-1 | 安全密码缺少使用范围开关 | 已修复 | 新增本地安全密码偏好，支持购买、取消订单、核销取货码独立开关 |
| MOD-1 | 0 元赠送与基础脏话未过滤 | 已修复 | 商品发布 / 编辑、聊天文本均接入本地关键词过滤，新增辱骂词与去空格/标点归一化匹配 |
| MOD-2 | 历史已发布违规商品仍可能显示 | 已修复 | API 启动时会把当前在售且命中本地违规词的历史商品自动转为下架 |
| MOD-3 | “代购”等违规词可通过 0 元赠送发布 | 已修复 | 本地违规词库新增代购、代买、代售、倒卖、黄牛等交易风险词，后端发布接口已验证 0 元商品同样拦截 |
| RENT-1 | 租赁商品租金 / 押金可输入非法字符或负数 | 已修复 | 前端输入框限制为合法金额格式；后端再次校验租金必须为大于 0 的数字，押金必须为 0 到 99999 的数字，最多两位小数 |
| AUTH-2 | 邮箱登录密码错误被当成登录过期 | 已修复 | 密码错误从 `401/4001` 改为 `400/4000` 业务错误，不会触发请求层清登录态和“登录已失效”弹窗 |
| STATS-6 | 运营报表周范围按 UTC 计算，CSV 可读性不足 | 已修复 | 报表日期分桶改为北京时间；CSV 新增 Campus 段，并将分类 / 学院枚举输出为名称 |
| AI-1 | AI 描述未结合商品图片 | 已修复，待外部 LLM 联调 | 发布页会把图片 URL 传给后端；后端 OpenAI 兼容请求支持 `image_url`，未配置或失败时降级本地文案 |
| AI-2 | 商品 / 聊天缺少可配置 LLM 内容审核接口 | 已接入，待外部 LLM 联调 | 新增 `IAiContentModerator`，配置 `AI_*` 后本地关键词未命中时调用外部模型二次审核 |
| DEMO-1 | 缺少统一测试账号 | 已修复 | 启动时确保 `admin@cau.edu.cn`、`demo1@cau.edu.cn`、`demo2@cau.edu.cn`、`l1user@cau.edu.cn` 存在，密码均为 `123456` |
| UI-1 | 华为等机型顶部标题不在绿色导航区域垂直居中 | 已修复 | 新增 `app-nav` 自定义导航组件，按状态栏与胶囊按钮高度计算导航高度；首页、商品、发布、消息、我的、登录及高频二级页均接入 |
| UI-2 | 发布商品页“立即发布”按钮在自定义 tabbar 下不可见或文字被截断 | 已修复 | 发布按钮改为页面流内提交区，底部预留 220rpx 安全空隙避开 `cover-view` tabbar，并限制按钮文字不换行/不截断 |
| STATS-7 | 管理端报表缺少直观的认证转化与趋势 UI | 已修复 | 仪表盘新增 L0/L1/L2 认证转化漏斗，运营报表新增近几日用户、发布、交易趋势条 |
| ITEM-7 | 发布商品不应强制至少上传 1 张图片 | 已修复 | 前端发布校验、后端发布校验、`ItemPublishDTOValidator` 均移除图片必填限制；仍保留最多 9 张限制，无图商品在列表 / 详情 / 订单中显示“暂无图片”占位 |
| RENT-2 | 租赁订单点击普通“核销取货码”会提示应使用租赁借出流程 | 已修复 | 我的订单按租赁状态区分按钮：卖家待借出显示“借出核销”，租赁中显示“归还确认码”；买家待借出显示“借出核销码”，租赁中显示“归还核销” |

## 3. 当前仍需外部配置 / 环境验收的问题

以下问题不影响本地功能测试，但仍需要外部配置或微信环境验收。

| 优先级 | 问题 | 当前状态 | 建议 |
|---|---|---|---|
| 高 | 微信订阅消息真实发送验收 | 待外部配置 | 需要把 AppID、AppSecret、购买成功模板 ID 填入本地 `.env`，再重启 API 联调 |
| 中 | 外部 LLM 真实调用验收 | 已有本地降级，待外部配置 | 当前不配置外部模型也可生成描述和本地过滤；如需真实 LLM，需要配置 `AI_ENABLED`、`AI_ENDPOINT`、`AI_API_KEY`、`AI_MODEL`，审核接口可选配 `AI_MODERATION_ENDPOINT` |

订阅消息说明：在微信开发者工具里点击“允许”通常只代表授权成功，不会像真机一样收到微信服务通知。真实购买成功消息需要在真机微信里查看“服务通知 / 订阅消息”，且必须满足 AppID、AppSecret、模板 ID、用户 openid、用户授权、后端发送成功这些条件。

## 4. 仍需微信开发者工具手工复测的流程

以下功能代码已修复或已实现，但仍建议在微信开发者工具中完整点测：

- 微信登录、邮箱 L1 认证、L2 认证提交与管理员审核。
- 设置安全密码、重置安全密码、登录密码重置。
- L1 发布 200 元以下商品、L1 发布高价商品被拒绝、L2 购买商品。
- 发布普通商品、关闭“可议价”后详情不显示“可议价”、发布成功后再次进入发布页表单为空。
- 发布租赁商品、发布 0 元赠送商品、编辑商品从租赁切换为非租赁。
- 发布 0 元赠送商品时，在标题或描述输入“代购”等违规词，应被拒绝发布。
- 发布租赁商品时，租金输入字母、负数、多个小数点、超过两位小数、0 都应被拒绝；押金输入字母、负数、超过两位小数应被拒绝。
- 浏览历史、我的订单、管理端商品列表、聊天列表、聊天详情中租赁商品均显示租金，不显示 `¥0.00`。
- 上传商品图片后检查图片可显示，并确认静态图片已合成 CAU 水印。
- 点击“智能生成”可生成商品描述；未配置外部 LLM 时应走本地降级文案。
- 购买商品后展示取货码、买家取消订单、卖家输入取货码和安全密码核销。
- 在账号设置中打开 / 关闭安全密码使用范围，分别验证购买、取消订单、核销取货码是否按开关弹出安全密码。
- 我的订单切换“全部 / 交易中 / 已完成 / 已取消”，确认列表内容随状态变化。
- 从我的订单联系卖家 / 买家，确认进入的是已有聊天记录。
- 聊天详情首条对方消息应显示在左侧。
- 新微信 L0 用户进入“我的”页后应能进入账号设置完成邮箱认证。
- 黑名单后商品不可见、聊天不可读写。
- 求购发布、重复求购拦截、“我有它”响应、发布者查看响应、续期、关闭。
- 管理员处理举报后扣信用分，重复处理同一举报应被拒绝。
- 管理员仪表盘运营报表：日/周/月/自定义筛选、校区筛选、搜索热词、页面点击、学院维度统计、CSV 复制导出。
- 配置微信订阅消息模板 ID 后，测试发布求购授权、卖家响应求购通知、新商品匹配求购通知。
- 配置购买成功模板 ID 后，测试首次购买订阅授权、再次购买不重复弹框，以及后端购买成功通知。
- 邮箱密码登录输错密码时，应只提示“邮箱或密码错误”，不应清空登录态或弹“登录已失效”。
- 管理员运营报表导出 CSV 后，确认周报日期按北京时间显示，CSV 包含 Summary、Daily、Campus、Category、TopItem、SearchKeyword、PageClick、College 段。
- 华为 / iPad 等模拟机检查首页、商品、发布、消息、我的、登录、注册与改密、商品详情、订单、管理后台等页面顶部标题是否在绿色导航区域内居中。
- 发布普通商品、0 元赠送商品、租赁商品时，滚动到页面底部确认“立即发布”按钮完整可见，不被底部 tabbar 遮挡，iPad Pro 下“立即发布”四个字不被截断。
- 不上传图片发布普通商品、0 元赠送商品、租赁商品，应允许发布；发布后商品列表、详情页、我的发布、我的订单应显示“暂无图片”占位。
- 租赁订单手测：买家购买租赁商品后在“我买到的”看到借出核销码；卖家在“我卖出的”点击“借出核销”，输入租期和买家借出码；卖家看到归还确认码；买家归还时点击“归还核销”，输入卖家归还确认码，订单变为已完成。
- 管理员仪表盘检查认证转化漏斗；运营报表检查近几日趋势条是否随数据展示。

## 5. 已执行的验证

本轮已执行以下验证：

```powershell
docker compose build api
docker compose up -d api
docker compose logs --tail=80 api
node --check miniprogram 下全部 JS 文件
git diff --check
```

验证结论：

- 后端 Docker 构建通过。
- API 容器可正常启动，`http://localhost:8080` 正常监听。
- `AppDbContext` pending changes 启动错误已消失。
- `t_stats_daily` 已由迁移创建。
- 管理员登录后可访问 `/api/v1/admin/stats/reports?period=week`。
- `/api/v1/admin/stats/export?period=week` 可返回 CSV 内容。
- 小程序 35 个 JS 文件语法检查通过。
- `git diff --check` 通过，仅有 Windows 环境下 LF/CRLF 换行提示。
- 2026-06-25 补充验证：发布页草稿 / 可议价修复、租赁价格展示修复后，`docker compose build api`、`docker compose up -d api`、`node --check` 全部通过，`http://localhost:8080/api/v1/items` 返回 `200 OK`。
- 2026-06-25 二次补充验证：安全密码可选开关、购买通知尽力发送、订单状态筛选、订单对方昵称、聊天首条方向、L2 学号重复校验、违规词过滤、AI 图片描述 / LLM 审核配置位、测试账号种子数据完成后，`docker compose build api` 通过，API 容器正常启动。
- 2026-06-25 二次补充验证：使用 `demo1@cau.edu.cn / 123456` 登录后发布标题 `nmsl` 的商品，接口返回 `400`，错误信息为“内容包含违规词「nmsl」，请修改后重试”。
- 2026-06-25 三次补充验证：使用 `demo1@cau.edu.cn / 123456` 登录后发布 0 元标题 `代购` 的商品，接口返回 `400`，错误信息为“内容包含违规词「代购」，请修改后重试”。
- 2026-06-25 三次补充验证：发布租赁商品且 `rentalRate=abc`，接口返回 `400`，错误信息为“请填写合法租金，租金必须为大于 0 的数字，最多两位小数”。
- 2026-06-25 三次补充验证：邮箱密码登录输入错误密码，接口返回 `400`，错误信息为“邮箱或密码错误”。
- 2026-06-25 三次补充验证：管理员导出 `period=week` 的 CSV，日期范围按北京时间变为 `2026-06-19` 至 `2026-06-25`，并包含新增的 Campus 段。
- 2026-06-25 四次补充验证：小程序 26 个 JSON 文件解析通过，39 个 JS 文件 `node --check` 通过，新增 `app-nav` 引用均已在页面 JSON 中注册。
- 2026-06-25 四次补充验证：`git diff --check` 通过；`docker compose build api` 通过；`dotnet format src/ --verify-no-changes --no-restore` 通过。

## 6. 当前提交建议

建议本次提交说明：

> fix: 修复内容过滤、租赁校验、聊天首条布局与报表日期问题

提交前注意：

- `.env` 不要提交。
- `project.private.config.json` 不要提交。
- `project.config.json` 如包含个人 AppID，建议继续保持忽略或仅提交团队确认过的配置。
- 微信订阅消息真实发送、外部 LLM 真实调用属于配置联调项，不建议混入本次提交。

# 校园二手交易小程序复测说明与当前问题清单

测试日期：2026-06-24

测试对象：`fix/testreport-issues` 分支当前本地代码

测试方式：对照测试报告、`docs/` 需求与设计文档进行代码复核；结合微信开发者工具现象进行问题定位；后端使用 Docker 构建与启动验证。

## 1. 测试依据

本次检查参考以下文档与测试记录：

- `TESTING_REPORT.md`
- `TESTING_REPORT_复测(1).md`
- `TESTING_REPORT_租赁_求购.md`
- `问题.md`
- `WORK_SUMMARY_fix-testreport-issues.md`
- `docs/SRS-CAU校园二手交易小程序.md`
- `docs/SDD-CAU校园二手交易小程序概要设计说明书.md`
- `docs/DDD-CAU校园二手交易小程序数据库设计说明书.md`
- `docs/SDDD-CAU校园二手交易小程序详细设计说明书(1).docx`

## 2. 本轮已修复 / 已融合的问题

### 2.1 小程序运行与显示问题

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| F-LOAD | 微信开发者工具报 `Identifier 'upload' has already been declared`，页面空白 | 已修复 | `miniprogram/api/files.js` 改为通过 `requestUtil.upload(...)` 调用，避免重复声明 |
| F-TIME | 新发布商品一直显示 `8小时前` | 已修复 | `formatTime()` 兼容后端返回的无时区 UTC 时间，避免按北京时间误解析 |
| R1 | 图片上传返回相对路径导致图片可能无法显示 | 已修复 | 上传接口 / 前端链路已能正常显示上传图片 |

### 2.2 认证、登录与权限

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| L2-1 | L2 学生认证缺少管理员审核闭环 | 已修复 | 用户可提交姓名、学号、证件照片；管理员可审核通过/驳回 |
| L2-2 | L2 提交审核后出现 `status of undefined` | 已修复 | 前端对返回结果做兜底，已有审核中申请会正常提示 |
| L2-3 | 管理端 L2 审核卡片显示邮箱未绑定 | 已修复 | 管理端审核列表可显示用户邮箱 |
| P1 | 安全密码设置后无法重置 | 已修复 | 注册与改密页新增重置安全密码入口和接口 |
| B3 | 封禁只影响重新登录，旧 token 仍可继续请求 | 已修复 | JWT 校验阶段检查用户是否已封禁；微信登录路径也检查封禁状态 |
| AUTH-1 | 忘记密码、注册账号误提示“请先登录” | 已修复 | 登录页入口可直接进入注册 / 重置流程 |

### 2.3 商品、租赁与发布

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| B6/F2 | 商品列表缺少成色筛选 | 已修复 | 后端 `ItemQuery` 支持 `ConditionLevel`，前端筛选面板已提供成色选项 |
| F1 | 首页分类第二次点击商品 Tab 不刷新 | 已修复 | 商品页 `onShow` 会读取分类筛选并刷新 |
| B5 | 发布页缺少租赁入口，编辑租赁字段不完整 | 已修复 | 发布 / 编辑页支持租赁开关、租金、押金；从租赁切换为非租赁会清空租赁字段 |
| ITEM-1 | L1 用户发布高价商品权限过宽 | 已修复 | L1 仅可发布 200 元以下商品；编辑商品时也校验目标价格 |
| ITEM-2 | 免费赠送快捷模式缺失 | 已修复 | 发布页新增“0元赠送”开关，开启后价格固定为 0 |

### 2.4 交易与风控

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| B2/F4 | 买家下单后拿不到取货码，订单页不展示取货码 | 已修复 | 创建交易返回取货码，订单页可展示 |
| B4 | 卖家核销取货码后商品没有变为 Sold | 已修复 | 核销成功后交易和商品状态会同步更新 |
| TX-1 | 取货码明文存储 | 已修复 | 新交易保存 HMAC 哈希；旧明文记录保留兼容校验与展示 |
| TX-2 | 敏感交易操作未强制安全密码 | 已修复 | 创建交易、取消交易、卖家核销、租赁开始、租赁归还均要求安全密码 |
| TX-3 | L2 完整交易权益未落实 | 已修复 | 购买商品要求 L2 用户 |
| F3 | “我的订单”联系按钮跳转 Tab 页无效 | 已修复 | 改为保存聊天参数后 `wx.switchTab()` 到消息页 |

### 2.5 求购模块

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| M3 | 求购模块有后端但没有前端入口 | 已修复 | 新增求购大厅、发布、响应、续期、关闭等前端流程 |
| REQ-1 | “我有它”只刷计数，没有真实响应闭环 | 已修复 | 响应时必须选择本人在售商品，发布者可查看响应商品 |
| REQ-2 | 求购响应可重复刷计数 | 已修复 | 服务端按 `(求购帖, 卖家, 商品)` 幂等去重 |
| REQ-3 | 求购重复发布检测缺失 | 已修复 | 24 小时内同一用户发布高度相似标题会被拒绝 |

### 2.6 管理后台、举报与黑名单

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| B7 | 管理员采纳举报不自动扣信用分 | 已修复 | 举报处理会写入信用记录并扣分 |
| REP-1 | 重复调用举报处理接口会重复扣分 | 已修复 | 已处理举报不可重复处理 |
| BLK-1 | 黑名单只保存关系，没有真正拦截 | 已修复 | 商品列表/详情、聊天会话、消息读取与发送均增加双向黑名单拦截 |
| EF-1 | API 启动提示 EF model pending changes | 已修复 | 补齐 `RequestResponse` 在迁移 designer 与 `AppDbContextModelSnapshot` 中的模型快照 |

### 2.7 数据统计与报表

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| STATS-1 | 数据统计报表只有简单仪表盘，缺少日/周/月/自定义区间 | 已修复 | `/api/v1/admin/stats/reports` 支持 `day/week/month/custom` 和校区筛选 |
| STATS-2 | 管理后台没有多维运营报表展示 | 已修复 | 管理员仪表盘新增运营报表区，展示新增用户、发布商品、完成交易、成交率、成交金额、活跃用户、校区对比、分类表现、热门商品 |
| STATS-3 | 报表不支持导出 | 已修复 | 新增 `/api/v1/admin/stats/export` CSV 导出；小程序端支持复制 CSV |
| STATS-4 | 每日 ETL 没有持久化落表 | 已修复 | 新增 `t_stats_daily`，`DailyEtlJob` 每日按东/西校区写入预聚合统计 |

### 2.8 微信订阅通知与求购匹配

| 编号 | 问题 | 当前状态 | 说明 |
|---|---|---|---|
| WX-1 | 求购响应后不通知求购发布者 | 已接入，待微信模板联调 | 响应成功后后端会调用微信订阅消息接口；未配置模板 ID 时自动跳过，不影响本地测试 |
| WX-2 | 新商品发布后不自动匹配求购 | 已接入，待微信模板联调 | 商品发布成功后会按校区、价格、资源类型和标题相似度匹配有效求购，并向买家发送订阅消息 |
| WX-3 | 小程序端没有请求订阅授权 | 已接入，待微信模板联调 | 发布求购前会请求“有人响应”和“新商品匹配”订阅授权；模板 ID 留空时自动跳过 |

## 3. 当前仍未修复 / 暂未实现的问题

以下问题仍然存在，建议后续单独建分支处理。

| 优先级 | 问题 | 当前状态 | 建议 |
|---|---|---|---|
| 高 | 微信订阅消息真实发送验收 | 待外部配置 | 需要在微信小程序后台申请模板，将模板 ID 填入后端 `WeChat:SubscribeTemplates` 和前端 `SUBSCRIBE_TEMPLATE_IDS` 再联调 |
| 中 | 页面点击量、搜索关键词云统计 | 未实现 | 需要先补 `t_event_log` / `t_search_log` 埋点采集，再接入报表 |
| 中 | 商品图片自动合成 CAU 水印 | 未实现 | 需要上传后图片处理流程 |
| 中 | AI 辅助生成商品描述 | 未实现 | 依赖外部 LLM/API 配置，当前未接入 |
| 低 | 学院标签筛选 | 未实现 | 需要用户学院字段、商品/求购筛选字段与前端入口配套 |

## 4. 仍需微信开发者工具手工复测的流程

以下功能代码已修复或已实现，但仍建议在微信开发者工具中完整点测：

- 微信登录、邮箱 L1 认证、L2 认证提交与管理员审核。
- 设置安全密码、重置安全密码、登录密码重置。
- L1 发布 200 元以下商品、L1 发布高价商品被拒绝、L2 购买商品。
- 发布普通商品、发布租赁商品、发布 0 元赠送商品、编辑商品从租赁切换为非租赁。
- 购买商品后展示取货码、买家取消订单、卖家输入取货码和安全密码核销。
- 黑名单后商品不可见、聊天不可读写。
- 求购发布、重复求购拦截、“我有它”响应、发布者查看响应、续期、关闭。
- 管理员处理举报后扣信用分，重复处理同一举报应被拒绝。
- 管理员仪表盘运营报表：日/周/月/自定义筛选、校区筛选、CSV 复制导出。
- 配置微信订阅消息模板 ID 后，测试发布求购授权、卖家响应求购通知、新商品匹配求购通知。

## 5. 已执行的验证

本轮已执行以下验证：

```powershell
docker compose build api
docker compose up -d api
docker compose logs --tail=80 api
node --check miniprogram 下全部 JS 文件
git diff --check
```

验证结论：

- 后端 Docker 构建通过。
- API 容器可正常启动，`http://localhost:8080` 正常监听。
- `AppDbContext` pending changes 启动错误已消失。
- `t_stats_daily` 已由迁移创建。
- 管理员登录后可访问 `/api/v1/admin/stats/reports?period=week`。
- `/api/v1/admin/stats/export?period=week` 可返回 CSV 内容。
- 小程序 31 个 JS 文件语法检查通过。
- `git diff --check` 通过，仅有 Windows 环境下 LF/CRLF 换行提示。

## 6. 当前提交建议

建议本次提交说明：

> fix: 修复测试报告遗留问题并补强交易权限与求购闭环

提交前注意：

- `.env` 不要提交。
- `project.private.config.json` 不要提交。
- `project.config.json` 如包含个人 AppID，建议继续保持忽略或仅提交团队确认过的配置。
- 数据统计、微信推送、水印、AI 描述、学院标签仍是后续待办，不建议混入本次提交。

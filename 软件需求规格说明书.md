# 校园二手交易平台 软件需求规格说明书

---

| 文档信息 | 内容 |
|---|---|
| 项目名称 | 校园二手交易平台 |
| 文档编号 | SRS-CST-2026-001 |
| 版本 | V1.0 |
| 编制日期 | 2026年5月6日 |
| 编制人 | —— |
| 审核人 | —— |
| 密级 | 公开 |

---

## 目录

- 1 引言
  - 1.1 目的
  - 1.2 范围
  - 1.3 定义、缩略语和缩写
  - 1.4 参考资料
  - 1.5 概述
- 2 总体描述
  - 2.1 产品前景
  - 2.2 产品功能
  - 2.3 用户特征
  - 2.4 运行环境
  - 2.5 一般约束
  - 2.6 假设和依赖
- 3 具体需求
  - 3.1 功能需求
    - 3.1.1 用户认证模块
    - 3.1.2 用户信息管理模块
    - 3.1.3 商品管理模块
    - 3.1.4 订单交易模块
    - 3.1.5 商品收藏模块
    - 3.1.6 站内聊天模块
    - 3.1.7 管理后台模块
    - 3.1.8 文件上传模块
  - 3.2 外部接口需求
    - 3.2.1 用户接口
    - 3.2.2 硬件接口
    - 3.2.3 软件接口
    - 3.2.4 通信接口
  - 3.3 性能需求
  - 3.4 设计约束
    - 3.4.1 标准符合性
    - 3.4.2 硬件约束
    - 3.4.3 技术约束
  - 3.5 软件系统属性
    - 3.5.1 可靠性
    - 3.5.2 安全性
    - 3.5.3 可维护性
    - 3.5.4 可移植性
    - 3.5.5 易用性
  - 3.6 数据需求
  - 3.7 其他需求
- 4 数据字典
- 5 验证需求
- 6 附录

---

## 1 引言

### 1.1 目的

本文档依据国家标准 GB/T 9385-2008《计算机软件需求规格说明规范》编制，旨在对"校园二手交易平台"的功能需求、接口需求、性能需求、设计约束及软件系统属性等方面进行完整、准确的描述，为后续的软件设计、编码、测试与验收提供基准依据。

本文档的预期读者包括：

- 项目开发人员（前端、后端、测试）
- 项目管理人员
- 课程评审教师

### 1.2 范围

**软件名称：** 校园二手交易平台

**软件用途：** 本系统是一个面向高校校园的二手物品交易系统，提供微信小程序客户端和 Node.js 后端服务。系统支持用户注册登录、商品发布与浏览、在线下单交易、订单管理、商品收藏、站内即时聊天以及管理员审核与后台统计等功能。

**主要目标：**

- 为校园用户提供便捷的二手商品发布、浏览和交易渠道
- 通过管理员审核机制保障商品信息的合规性
- 通过订单状态流转实现完整的二手交易闭环
- 提供站内聊天功能方便买卖双方沟通

### 1.3 定义、缩略语和缩写

| 术语/缩写 | 全称/定义 |
|---|---|
| SRS | Software Requirements Specification，软件需求规格说明 |
| JWT | JSON Web Token，一种基于 JSON 的开放标准令牌格式，用于身份认证 |
| ORM | Object-Relational Mapping，对象关系映射 |
| API | Application Programming Interface，应用程序编程接口 |
| REST | Representational State Transfer，表述性状态转移 |
| 小程序 | 微信小程序，一种无需下载安装即可使用的轻应用 |
| 买家 | 注册角色为 buyer 的用户，具有购买和收藏商品的权限 |
| 卖家 | 注册角色为 seller 的用户，具有发布商品和处理订单的权限 |
| 管理员 | 角色为 admin 的用户，具有审核商品、管理用户、查看统计的权限 |
| Sequelize | Node.js 环境下的 ORM 框架，用于操作 MySQL 数据库 |

### 1.4 参考资料

| 序号 | 资料名称 | 版本/编号 |
|---|---|---|
| 1 | GB/T 9385-2008 计算机软件需求规格说明规范 | 2008版 |
| 2 | GB/T 8567-2006 计算机软件文档编制规范 | 2006版 |
| 3 | 微信小程序开发文档 | —— |
| 4 | Express.js 官方文档 | V4.x |
| 5 | Sequelize 官方文档 | V6.x |

### 1.5 概述

本文档第 2 章对产品进行总体描述，包括产品前景、功能概述、用户特征、运行环境和约束条件；第 3 章详细列出各项具体需求，包括功能需求、外部接口需求、性能需求、设计约束和软件系统属性；第 4 章提供数据字典；第 5 章列出验证需求；第 6 章为附录。

---

## 2 总体描述

### 2.1 产品前景

校园二手交易平台是一个独立的 B/S 架构系统，采用前后端分离设计。前端为微信小程序客户端，后端为 Node.js + Express 服务，数据持久化层采用 MySQL 数据库配合 Sequelize ORM。

系统与其他外部系统的关系如下：

```
┌──────────────────┐       ┌──────────────────┐       ┌──────────────────┐
│  微信小程序客户端  │──────▶│  Node.js 后端服务  │──────▶│   MySQL 数据库    │
│  (WXML/WXSS/JS)  │◀──────│  (Express REST   │◀──────│  (Sequelize ORM) │
│                  │  HTTP  │   API)           │  SQL  │                  │
└──────────────────┘       └──────────────────┘       └──────────────────┘
        │                          │
        │ wx.login()               │ multer (文件上传)
        ▼                          ▼
┌──────────────────┐       ┌──────────────────┐
│  微信开放平台      │       │  本地文件存储      │
│  (OpenID 服务)    │       │  (/uploads/)      │
└──────────────────┘       └──────────────────┘
```

### 2.2 产品功能

本系统包含以下主要功能模块：

| 模块编号 | 模块名称 | 功能简述 |
|---|---|---|
| M01 | 用户认证模块 | 注册、登录、微信登录、修改密码 |
| M02 | 用户信息管理模块 | 查看个人资料、编辑资料 |
| M03 | 商品管理模块 | 商品发布、编辑、下架、浏览、搜索筛选 |
| M04 | 订单交易模块 | 下单、卖家确认、买家确认收货、取消订单 |
| M05 | 商品收藏模块 | 收藏/取消收藏商品、查看收藏列表 |
| M06 | 站内聊天模块 | 会话列表、查看消息、发送消息 |
| M07 | 管理后台模块 | 用户管理（封禁/解封）、商品审核、数据统计 |
| M08 | 文件上传模块 | 商品图片上传 |

### 2.3 用户特征

本系统的用户分为以下三类：

| 角色 | 特征描述 |
|---|---|
| 买家（buyer） | 高校在校学生，具备基本的手机操作能力。主要使用浏览商品、搜索、下单购买、收藏、聊天等功能。注册时选择"买家"角色。 |
| 卖家（seller） | 高校在校学生，具备基本的手机操作能力。除买家功能外，还具有发布商品、编辑商品、查看卖家订单、确认订单等额外权限。注册时选择"卖家"角色。 |
| 管理员（admin） | 系统预设账号，负责平台内容管理。具有审核商品上下架、封禁/解封用户账号、查看平台数据统计等权限。 |

### 2.4 运行环境

| 项目 | 要求 |
|---|---|
| 前端运行环境 | 微信客户端（iOS / Android），通过微信小程序运行 |
| 后端操作系统 | macOS / Windows / Linux |
| 后端运行时 | Node.js ≥ 18 |
| 数据库 | MySQL ≥ 8.0 |
| 开发工具 | 微信开发者工具（小程序端） |

### 2.5 一般约束

- 用户必须通过微信客户端访问小程序前端
- 商品发布需经过管理员审核后才能在商品列表中公开展示
- 每个商品同时只能存在一笔进行中的订单（待确认或已确认状态）
- 用户不可购买自己发布的商品
- 商品图片数量上限为 6 张
- JWT 令牌有效期为 7 天

### 2.6 假设和依赖

- 假设用户使用的微信客户端版本支持小程序运行
- 假设后端服务器能够正常连接 MySQL 数据库
- 依赖微信开放平台提供的 OpenID 服务以支持微信登录
- 假设后端首次启动时通过 Sequelize 的 `sync({ alter: true })` 自动创建数据库表结构
- 修改密码的短信验证码当前为演示模式（固定验证码 `123456`），正式部署时需对接短信服务商

---

## 3 具体需求

### 3.1 功能需求

#### 3.1.1 用户认证模块（M01）

##### FR-01-001 用户注册

| 属性 | 描述 |
|---|---|
| 标识 | FR-01-001 |
| 功能描述 | 买家或卖家通过填写必要信息注册为系统用户 |
| 输入 | 昵称（nickname）、手机号（phone）、密码（password）、角色（role）、学号（student_id）、学院（college） |
| 处理 | 1. 校验所有必填字段非空；2. 校验角色只能为 buyer 或 seller；3. 检查手机号是否已注册；4. 使用 bcrypt（salt=10）对密码进行哈希加密；5. 创建用户记录；6. 生成 JWT 令牌并返回 |
| 输出 | 注册成功返回 JWT 令牌及用户基本信息；失败返回错误码和错误信息 |
| 前置条件 | 手机号未被注册 |
| 后置条件 | 数据库新增一条用户记录 |
| 异常情况 | - 参数缺失：返回 400 错误；- 手机号已注册：返回 400 错误"手机号已注册"；- 服务端异常：返回 500 错误 |

##### FR-01-002 用户登录

| 属性 | 描述 |
|---|---|
| 标识 | FR-01-002 |
| 功能描述 | 已注册用户通过手机号和密码登录 |
| 输入 | 手机号（phone）、密码（password） |
| 处理 | 1. 校验参数非空；2. 查询手机号对应的用户；3. 校验用户存在且未被封禁；4. 使用 bcrypt 比对密码哈希；5. 生成 JWT 令牌并返回 |
| 输出 | 登录成功返回 JWT 令牌及用户基本信息；失败返回错误码和错误信息 |
| 前置条件 | 用户已注册且账号未被封禁 |
| 后置条件 | 无 |
| 异常情况 | - 用户不存在：返回 404；- 账号被封禁：返回 403；- 密码错误：返回 401；- 参数缺失：返回 400 |

##### FR-01-003 微信登录

| 属性 | 描述 |
|---|---|
| 标识 | FR-01-003 |
| 功能描述 | 用户通过微信授权快速登录系统 |
| 输入 | wx_openid、nickname（可选）、phone（可选） |
| 处理 | 1. 校验 wx_openid 非空；2. 根据 wx_openid 查询用户；3. 若未找到但提供了手机号，尝试绑定已有账号；4. 若仍未找到，自动创建新用户（角色默认为 buyer）；5. 校验用户未被封禁；6. 生成 JWT 令牌并返回 |
| 输出 | 登录成功返回 JWT 令牌及用户基本信息 |
| 前置条件 | 微信客户端已授权获取 openid |
| 后置条件 | 新用户登录时自动创建用户记录 |
| 异常情况 | - 账号被封禁：返回 403；- 参数缺失：返回 400 |

##### FR-01-004 修改密码

| 属性 | 描述 |
|---|---|
| 标识 | FR-01-004 |
| 功能描述 | 用户通过手机验证码验证身份后修改密码 |
| 输入 | 手机号（phone）、验证码（code）、新密码（newPassword） |
| 处理 | 1. 校验参数非空；2. 校验验证码正确（演示版固定为 123456）；3. 查询手机号对应的用户；4. 使用 bcrypt 对新密码哈希加密；5. 更新数据库密码字段 |
| 输出 | 修改成功返回成功信息；失败返回错误信息 |
| 前置条件 | 用户已注册 |
| 后置条件 | 数据库中对应用户的密码字段被更新 |
| 异常情况 | - 验证码错误：返回 400；- 用户不存在：返回 404 |

#### 3.1.2 用户信息管理模块（M02）

##### FR-02-001 查看个人资料

| 属性 | 描述 |
|---|---|
| 标识 | FR-02-001 |
| 功能描述 | 登录用户查看自己的个人资料信息 |
| 输入 | JWT 令牌（通过 Authorization 请求头传递） |
| 处理 | 1. 验证 JWT 令牌有效性；2. 解析用户 ID；3. 返回用户信息（不含密码） |
| 输出 | 用户信息对象：id、nickname、phone、avatar、role、student_id、college、wx_openid、is_banned、created_at |
| 前置条件 | 用户已登录 |
| 后置条件 | 无 |

##### FR-02-002 编辑个人资料

| 属性 | 描述 |
|---|---|
| 标识 | FR-02-002 |
| 功能描述 | 登录用户修改自己的昵称、头像、学院、手机号等信息 |
| 输入 | nickname（可选）、avatar（可选）、college（可选）、phone（可选） |
| 处理 | 1. 验证 JWT 令牌有效性；2. 若修改手机号，检查新手机号是否已被其他用户使用；3. 更新对应字段 |
| 输出 | 更新后的用户信息 |
| 前置条件 | 用户已登录 |
| 后置条件 | 数据库中对应用户的资料被更新 |
| 异常情况 | - 手机号已被使用：返回 400 |

#### 3.1.3 商品管理模块（M03）

##### FR-03-001 浏览商品列表

| 属性 | 描述 |
|---|---|
| 标识 | FR-03-001 |
| 功能描述 | 所有用户（含未登录）可浏览已审核通过的商品列表，支持多条件筛选和分页 |
| 输入 | keyword（关键词，可选）、categoryId（分类ID，可选）、priceMin（最低价，可选）、priceMax（最高价，可选）、condition（成色ID，可选）、sort（排序方式：new/price_asc/price_desc/view_desc）、page（页码，默认1）、pageSize（每页数量，默认10） |
| 处理 | 1. 构建筛选条件（review_status='approved' 且 status≠'offline'）；2. 按关键词模糊匹配标题；3. 按分类、成色、价格范围精确筛选；4. 按指定字段排序；5. 执行分页查询；6. 返回商品列表及分页信息 |
| 输出 | 商品列表（list）、总记录数（total）、当前页码（page）、每页数量（pageSize） |
| 前置条件 | 无（无需登录） |
| 后置条件 | 无 |

##### FR-03-002 查看商品详情

| 属性 | 描述 |
|---|---|
| 标识 | FR-03-002 |
| 功能描述 | 查看指定商品的详细信息，同时增加浏览次数 |
| 输入 | 商品 ID（路径参数） |
| 处理 | 1. 根据 ID 查询商品及卖家信息；2. 商品浏览次数（view_count）加 1；3. 返回商品详情 |
| 输出 | 商品完整信息，包含卖家信息（id、nickname、avatar、college、phone） |
| 前置条件 | 商品存在 |
| 后置条件 | 商品浏览计数器加 1 |
| 异常情况 | - 商品不存在：返回 404 |

##### FR-03-003 发布商品

| 属性 | 描述 |
|---|---|
| 标识 | FR-03-003 |
| 功能描述 | 卖家或管理员发布新商品 |
| 输入 | title（标题）、images（图片数组）、price（价格）、category_id（分类ID）、condition（成色ID）、description（描述） |
| 处理 | 1. 验证用户角色为 seller 或 admin；2. 校验必填参数及图片数量≤6；3. 创建商品记录（review_status 默认为 pending，admin 发布则直接 approved） |
| 输出 | 创建的商品信息 |
| 前置条件 | 用户已登录且角色为 seller 或 admin |
| 后置条件 | 数据库新增商品记录，状态为待审核（admin 发布则为已审核） |
| 异常情况 | - 角色权限不足：返回 403；- 参数缺失：返回 400；- 图片超限：返回 400 |

##### FR-03-004 编辑商品

| 属性 | 描述 |
|---|---|
| 标识 | FR-03-004 |
| 功能描述 | 卖家编辑自己已发布的商品信息 |
| 输入 | title、images、price、category_id、condition、description、status（均可选） |
| 处理 | 1. 验证商品存在；2. 验证操作者为商品卖家；3. 更新商品字段；4. 将审核状态重置为 pending |
| 输出 | 更新后的商品信息 |
| 前置条件 | 用户已登录且为商品卖家 |
| 后置条件 | 商品信息更新，审核状态重置为待审核 |
| 异常情况 | - 商品不存在：返回 404；- 无权限：返回 403 |

##### FR-03-005 下架商品

| 属性 | 描述 |
|---|---|
| 标识 | FR-03-005 |
| 功能描述 | 商品卖家或管理员将商品下架（逻辑删除） |
| 输入 | 商品 ID（路径参数） |
| 处理 | 1. 验证商品存在；2. 验证操作者为商品卖家或管理员；3. 将商品状态设为 offline |
| 输出 | 下架成功信息 |
| 前置条件 | 用户已登录且为商品卖家或管理员 |
| 后置条件 | 商品状态变更为 offline，不再在商品列表中展示 |
| 异常情况 | - 商品不存在：返回 404；- 无权限：返回 403 |

##### FR-03-006 查看我的商品

| 属性 | 描述 |
|---|---|
| 标识 | FR-03-006 |
| 功能描述 | 卖家查看自己发布的所有商品列表 |
| 输入 | JWT 令牌 |
| 处理 | 根据当前用户 ID 查询其所有商品，按创建时间倒序排列 |
| 输出 | 当前用户发布的商品列表 |
| 前置条件 | 用户已登录 |
| 后置条件 | 无 |

##### FR-03-007 搜索商品

| 属性 | 描述 |
|---|---|
| 标识 | FR-03-007 |
| 功能描述 | 通过关键词模糊搜索商品标题 |
| 输入 | keyword（关键词） |
| 处理 | 对商品标题进行 LIKE 模糊匹配，结合 FR-03-001 的筛选逻辑 |
| 输出 | 匹配的商品列表 |
| 前置条件 | 无 |
| 后置条件 | 无 |

#### 3.1.4 订单交易模块（M04）

##### FR-04-001 创建订单（下单）

| 属性 | 描述 |
|---|---|
| 标识 | FR-04-001 |
| 功能描述 | 买家对可购买的商品下单 |
| 输入 | goods_id（商品ID） |
| 处理 | 1. 验证商品存在且状态为 available、审核通过；2. 验证买家不能购买自己的商品；3. 验证该商品无进行中的订单；4. 创建订单（状态为 pending） |
| 输出 | 创建的订单信息 |
| 前置条件 | 用户已登录，商品在售且审核通过，无进行中订单 |
| 后置条件 | 数据库新增订单记录 |
| 异常情况 | - 商品不存在：返回 404；- 商品不可购买：返回 400；- 不能购买自己的商品：返回 403；- 已有进行中订单：返回 400 |

##### FR-04-002 查看买家订单列表

| 属性 | 描述 |
|---|---|
| 标识 | FR-04-002 |
| 功能描述 | 买家查看自己所有购买订单 |
| 输入 | JWT 令牌 |
| 处理 | 根据 buyer_id 查询订单，关联商品信息和卖家信息，按更新时间倒序 |
| 输出 | 订单列表，包含商品信息和卖家信息 |
| 前置条件 | 用户已登录 |
| 后置条件 | 无 |

##### FR-04-003 查看卖家订单列表

| 属性 | 描述 |
|---|---|
| 标识 | FR-04-003 |
| 功能描述 | 卖家查看自己所有销售订单 |
| 输入 | JWT 令牌 |
| 处理 | 根据 seller_id 查询订单，关联商品信息和买家信息，按更新时间倒序 |
| 输出 | 订单列表，包含商品信息和买家信息 |
| 前置条件 | 用户已登录 |
| 后置条件 | 无 |

##### FR-04-004 查看订单详情

| 属性 | 描述 |
|---|---|
| 标识 | FR-04-004 |
| 功能描述 | 查看指定订单的完整详情 |
| 输入 | 订单 ID（路径参数） |
| 处理 | 1. 查询订单及关联的商品、买家、卖家信息；2. 验证操作者为买家、卖家或管理员 |
| 输出 | 订单完整信息 |
| 前置条件 | 用户已登录且为订单相关方或管理员 |
| 后置条件 | 无 |
| 异常情况 | - 订单不存在：返回 404；- 无权限：返回 403 |

##### FR-04-005 卖家确认订单

| 属性 | 描述 |
|---|---|
| 标识 | FR-04-005 |
| 功能描述 | 卖家确认买家的购买订单 |
| 输入 | 订单 ID（路径参数） |
| 处理 | 1. 验证订单存在；2. 验证操作者为卖家；3. 验证订单状态为 pending；4. 将订单状态变更为 confirmed |
| 输出 | 确认后的订单信息 |
| 前置条件 | 用户已登录，为订单卖家，订单状态为 pending |
| 后置条件 | 订单状态变更为 confirmed |
| 异常情况 | - 订单不存在：返回 404；- 非卖家操作：返回 403；- 状态不允许确认：返回 400 |

##### FR-04-006 取消订单

| 属性 | 描述 |
|---|---|
| 标识 | FR-04-006 |
| 功能描述 | 买家或卖家取消订单 |
| 输入 | 订单 ID（路径参数）、cancel_reason（取消原因，可选） |
| 处理 | 1. 验证订单存在；2. 验证操作者为买家或卖家；3. 验证订单未完结；4. 将订单状态变更为 cancelled，记录取消原因 |
| 输出 | 取消后的订单信息 |
| 前置条件 | 用户已登录，为订单买家或卖家，订单未完结 |
| 后置条件 | 订单状态变更为 cancelled |
| 异常情况 | - 订单不存在：返回 404；- 无权限：返回 403；- 订单已结束：返回 400 |

##### FR-04-007 确认收货

| 属性 | 描述 |
|---|---|
| 标识 | FR-04-007 |
| 功能描述 | 买家确认收货，完成交易 |
| 输入 | 订单 ID（路径参数） |
| 处理 | 1. 验证订单存在；2. 验证操作者为买家；3. 验证订单状态为 confirmed；4. 将订单状态变更为 completed；5. 将商品状态变更为 sold |
| 输出 | 完成后的订单信息 |
| 前置条件 | 用户已登录，为订单买家，订单状态为已确认 |
| 后置条件 | 订单状态变更为 completed，商品状态变更为 sold |
| 异常情况 | - 订单不存在：返回 404；- 非买家操作：返回 403；- 状态不允许完成：返回 400 |

#### 3.1.5 商品收藏模块（M05）

##### FR-05-001 收藏商品

| 属性 | 描述 |
|---|---|
| 标识 | FR-05-001 |
| 功能描述 | 登录用户收藏指定商品 |
| 输入 | 商品 ID（路径参数） |
| 处理 | 1. 验证商品存在；2. 对 user_id 和 goods_id 组合执行 findOrCreate 操作（避免重复收藏） |
| 输出 | 收藏记录信息 |
| 前置条件 | 用户已登录 |
| 后置条件 | 新增收藏记录（若之前未收藏） |

##### FR-05-002 取消收藏

| 属性 | 描述 |
|---|---|
| 标识 | FR-05-002 |
| 功能描述 | 登录用户取消收藏指定商品 |
| 输入 | 商品 ID（路径参数） |
| 处理 | 根据 user_id 和 goods_id 删除收藏记录 |
| 输出 | 取消收藏成功信息 |
| 前置条件 | 用户已登录 |
| 后置条件 | 删除对应收藏记录 |

##### FR-05-003 查看收藏列表

| 属性 | 描述 |
|---|---|
| 标识 | FR-05-003 |
| 功能描述 | 登录用户查看自己收藏的所有商品 |
| 输入 | JWT 令牌 |
| 处理 | 根据 user_id 查询收藏记录，关联商品信息，按创建时间倒序 |
| 输出 | 收藏列表，包含完整商品信息 |
| 前置条件 | 用户已登录 |
| 后置条件 | 无 |

#### 3.1.6 站内聊天模块（M06）

##### FR-06-001 获取会话列表

| 属性 | 描述 |
|---|---|
| 标识 | FR-06-001 |
| 功能描述 | 登录用户查看与所有其他用户的聊天会话列表 |
| 输入 | JWT 令牌 |
| 处理 | 1. 查询当前用户参与的所有消息（作为发送者或接收者）；2. 按对方用户 ID + 商品 ID 分组聚合为会话；3. 每个会话展示对方用户信息、最后一条消息内容和时间 |
| 输出 | 会话列表，每项包含：conversationId、goods_id、lastMessage、lastTime、otherUser |
| 前置条件 | 用户已登录 |
| 后置条件 | 无 |

##### FR-06-002 获取消息列表

| 属性 | 描述 |
|---|---|
| 标识 | FR-06-002 |
| 功能描述 | 查看与指定用户在指定商品下的聊天消息记录 |
| 输入 | conversationId（格式：{otherId}_{goodsId}，路径参数） |
| 处理 | 1. 解析会话 ID 获取对方用户 ID 和商品 ID；2. 查询双方之间的消息记录，按创建时间正序排列；3. 将对方发来的未读消息标记为已读 |
| 输出 | 消息列表，按时间正序排列 |
| 前置条件 | 用户已登录 |
| 后置条件 | 未读消息标记为已读 |

##### FR-06-003 发送消息

| 属性 | 描述 |
|---|---|
| 标识 | FR-06-003 |
| 功能描述 | 登录用户向另一用户发送关于某商品的文字消息 |
| 输入 | receiver_id（接收者ID）、goods_id（商品ID）、content（消息内容） |
| 处理 | 1. 校验参数非空；2. 创建消息记录（默认未读） |
| 输出 | 发送的消息信息 |
| 前置条件 | 用户已登录 |
| 后置条件 | 新增消息记录 |

#### 3.1.7 管理后台模块（M07）

##### FR-07-001 获取用户列表

| 属性 | 描述 |
|---|---|
| 标识 | FR-07-001 |
| 功能描述 | 管理员查看系统所有用户信息 |
| 输入 | JWT 令牌（管理员角色） |
| 处理 | 查询所有用户信息（排除密码字段），按创建时间倒序 |
| 输出 | 用户列表 |
| 前置条件 | 用户已登录且角色为 admin |
| 后置条件 | 无 |

##### FR-07-002 封禁/解封用户

| 属性 | 描述 |
|---|---|
| 标识 | FR-07-002 |
| 功能描述 | 管理员切换指定用户的封禁状态 |
| 输入 | 用户 ID（路径参数） |
| 处理 | 1. 验证目标用户存在；2. 验证目标用户不是管理员；3. 切换 is_banned 字段 |
| 输出 | 操作结果（用户ID及新的封禁状态） |
| 前置条件 | 用户已登录且角色为 admin |
| 后置条件 | 目标用户封禁状态被切换 |
| 异常情况 | - 用户不存在：返回 404；- 不可封禁管理员：返回 400 |

##### FR-07-003 获取待审核商品列表

| 属性 | 描述 |
|---|---|
| 标识 | FR-07-003 |
| 功能描述 | 管理员查看所有待审核的商品 |
| 输入 | JWT 令牌（管理员角色） |
| 处理 | 查询 review_status='pending' 的商品，按创建时间倒序 |
| 输出 | 待审核商品列表 |
| 前置条件 | 用户已登录且角色为 admin |
| 后置条件 | 无 |

##### FR-07-004 审核商品

| 属性 | 描述 |
|---|---|
| 标识 | FR-07-004 |
| 功能描述 | 管理员对商品进行审核操作（通过/拒绝/删除） |
| 输入 | 商品 ID（路径参数）、action（操作：approve/reject/delete）、reason（拒绝原因，可选） |
| 处理 | 1. 验证商品存在；2. 若 action 为 approve：设置 review_status='approved'，清除拒绝原因；3. 若 action 为 reject：设置 review_status='rejected'，商品状态设为 offline，记录拒绝原因；4. 若 action 为 delete：物理删除商品记录 |
| 输出 | 审核结果 |
| 前置条件 | 用户已登录且角色为 admin |
| 后置条件 | 商品审核状态或记录被更新/删除 |
| 异常情况 | - 商品不存在：返回 404；- 无效操作：返回 400 |

##### FR-07-005 平台数据统计

| 属性 | 描述 |
|---|---|
| 标识 | FR-07-005 |
| 功能描述 | 管理员查看平台运营数据统计 |
| 输入 | type（统计周期：day/week/month，默认 day）、start（开始日期，可选）、end（结束日期，可选） |
| 处理 | 1. 根据统计类型或自定义日期范围构建时间筛选条件；2. 统计新增用户数、新增商品数、新增订单数、已完成订单数、已完成订单总金额 |
| 输出 | 统计结果：userCount、goodsCount、orderCount、doneCount、totalAmount |
| 前置条件 | 用户已登录且角色为 admin |
| 后置条件 | 无 |

#### 3.1.8 文件上传模块（M08）

##### FR-08-001 上传图片

| 属性 | 描述 |
|---|---|
| 标识 | FR-08-001 |
| 功能描述 | 登录用户上传商品图片 |
| 输入 | file（图片文件，通过 multipart/form-data 上传） |
| 处理 | 1. 验证用户已登录；2. 使用 multer 中间件处理文件上传；3. 存储到服务器本地 uploads 目录；4. 返回文件访问 URL |
| 输出 | 上传成功的图片 URL |
| 前置条件 | 用户已登录 |
| 后置条件 | 图片文件存储到服务器本地 |

---

### 3.2 外部接口需求

#### 3.2.1 用户接口

**小程序页面导航结构：**

| 页面 | 路径 | 说明 |
|---|---|---|
| 登录页 | /pages/login/login | 应用入口页 |
| 注册页 | /pages/register/register | 新用户注册 |
| 首页 | /pages/index/index | Tab 页：展示分类入口、热门商品、最新商品 |
| 商品列表页 | /pages/goods/goods | Tab 页：展示商品列表，支持搜索、筛选、排序 |
| 商品详情页 | /pages/goods-detail/goods-detail | 展示商品详细信息，提供下单/收藏/聊天入口 |
| 发布页 | /pages/publish/publish | Tab 页：发布或编辑商品 |
| 我的页面 | /pages/my/my | Tab 页：个人中心入口 |
| 我的订单页 | /pages/my-orders/my-orders | 查看买家/卖家订单列表 |
| 管理后台页 | /pages/admin/admin | 仅管理员可见：用户管理、商品审核、数据统计 |
| 聊天页 | /pages/chat/chat | 查看会话列表及聊天消息 |

**Tab 栏结构：**

| Tab 项 | 页面 | 图标/文字 |
|---|---|---|
| 首页 | pages/index/index | 首页 |
| 商品 | pages/goods/goods | 商品 |
| 发布 | pages/publish/publish | 发布 |
| 我的 | pages/my/my | 我的 |

**交互要求：**

- 商品列表支持下拉刷新和上拉加载更多
- 商品详情页提供"立即购买""收藏""联系卖家"操作按钮
- 图片上传使用系统媒体选择器，最多选择 6 张
- 表单输入时实时校验并给出提示

#### 3.2.2 硬件接口

- 本系统为纯软件系统，无特殊硬件接口需求
- 需要用户设备具有网络连接能力（Wi-Fi 或移动数据）

#### 3.2.3 软件接口

| 接口 | 说明 |
|---|---|
| MySQL 数据库 | 后端通过 Sequelize ORM 连接 MySQL 数据库（campus_secondhand），使用 TCP/IP 协议，默认端口 3306 |
| 微信开放平台 | 通过 wx.login() 获取用户 openid，用于微信登录功能 |
| 文件系统 | 使用 multer 中间件将上传的图片存储到服务器本地文件系统 |

#### 3.2.4 通信接口

**RESTful API 接口规范：**

- 协议：HTTP
- 基础路径：`/api`
- 认证方式：JWT Bearer Token（通过 Authorization 请求头传递）
- 数据格式：JSON（Content-Type: application/json）
- 文件上传：multipart/form-data

**统一响应格式：**

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

- 成功时 `code` 为 `0`
- 失败时 `code` 为对应的 HTTP 状态码，`message` 为错误描述

**API 接口清单：**

| 模块 | 方法 | 路径 | 说明 | 认证 |
|---|---|---|---|---|
| 认证 | POST | /api/auth/register | 用户注册 | 否 |
| 认证 | POST | /api/auth/login | 用户登录 | 否 |
| 认证 | POST | /api/auth/wx-login | 微信登录 | 否 |
| 认证 | PUT | /api/auth/password | 修改密码 | 否 |
| 用户 | GET | /api/user/profile | 获取个人资料 | 是 |
| 用户 | PUT | /api/user/profile | 更新个人资料 | 是 |
| 商品 | GET | /api/goods | 获取商品列表（带筛选/分页） | 否 |
| 商品 | GET | /api/goods/:id | 获取商品详情 | 否 |
| 商品 | POST | /api/goods | 发布商品 | 是 |
| 商品 | PUT | /api/goods/:id | 编辑商品 | 是 |
| 商品 | DELETE | /api/goods/:id | 下架商品 | 是 |
| 商品 | GET | /api/goods/my | 获取我的商品 | 是 |
| 商品 | POST | /api/goods/:id/favor | 收藏商品 | 是 |
| 商品 | DELETE | /api/goods/:id/favor | 取消收藏 | 是 |
| 商品 | GET | /api/goods/favor/list | 收藏列表 | 是 |
| 订单 | POST | /api/orders | 创建订单 | 是 |
| 订单 | GET | /api/orders/buyer | 买家订单列表 | 是 |
| 订单 | GET | /api/orders/seller | 卖家订单列表 | 是 |
| 订单 | GET | /api/orders/:id | 订单详情 | 是 |
| 订单 | PUT | /api/orders/:id/confirm | 卖家确认订单 | 是 |
| 订单 | PUT | /api/orders/:id/cancel | 取消订单 | 是 |
| 订单 | PUT | /api/orders/:id/complete | 确认收货 | 是 |
| 管理 | GET | /api/admin/users | 用户列表 | 管理员 |
| 管理 | PUT | /api/admin/users/:id/toggle | 封禁/解封用户 | 管理员 |
| 管理 | GET | /api/admin/goods/pending | 待审核商品 | 管理员 |
| 管理 | PUT | /api/admin/goods/:id/review | 审核商品 | 管理员 |
| 管理 | GET | /api/admin/stats | 平台统计 | 管理员 |
| 聊天 | GET | /api/chat/conversations | 会话列表 | 是 |
| 聊天 | GET | /api/chat/messages/:conversationId | 消息列表 | 是 |
| 聊天 | POST | /api/chat/messages | 发送消息 | 是 |
| 上传 | POST | /api/upload/image | 上传图片 | 是 |

---

### 3.3 性能需求

| 编号 | 需求描述 |
|---|---|
| PR-01 | 系统应支持并发访问。在校园使用场景下（预计同时在线用户数 ≤ 500），API 平均响应时间应不超过 2 秒 |
| PR-02 | 商品列表查询（含筛选和分页）响应时间应不超过 1 秒 |
| PR-03 | 图片上传单张文件大小应支持不超过 5MB |
| PR-04 | 数据库查询应充分利用索引，避免全表扫描 |
| PR-05 | JWT 令牌有效期设为 7 天，过期后需重新登录 |

### 3.4 设计约束

#### 3.4.1 标准符合性

- 接口设计遵循 RESTful API 设计规范
- 密码存储使用 bcrypt 哈希加密算法（salt rounds = 10）
- 身份认证采用 JWT（JSON Web Token）标准
- 数据库字符集采用 utf8mb4，支持 emoji 等特殊字符

#### 3.4.2 硬件约束

- 无特殊硬件要求，普通服务器或个人电脑即可运行后端服务
- 客户端为微信小程序，运行于支持微信的移动设备

#### 3.4.3 技术约束

| 层级 | 技术选型 | 约束说明 |
|---|---|---|
| 前端 | 微信小程序原生开发（WXML + WXSS + JS） | 必须在微信开发者工具中编译运行 |
| 后端 | Node.js + Express | 版本 ≥ 18 |
| 数据库 | MySQL + Sequelize ORM | MySQL ≥ 8.0 |
| 认证 | JWT | 通过 express 中间件实现 |
| 文件上传 | multer | 存储到本地 uploads 目录 |

### 3.5 软件系统属性

#### 3.5.1 可靠性

- 后端 API 接口统一进行 try-catch 异常捕获，服务端异常返回 500 错误，不泄露内部错误详情
- 数据库通过 Sequelize ORM 的 sync 机制确保表结构与模型一致
- 订单操作（确认、取消、完成）均校验当前订单状态，防止非法状态流转

#### 3.5.2 安全性

- 用户密码使用 bcrypt（salt=10）进行不可逆哈希加密存储
- 所有需要身份认证的接口通过 JWT 中间件验证令牌有效性
- 管理员接口通过额外的角色校验中间件（requireAdmin）保护
- 商品操作（编辑、下架）校验操作者是否为商品所有者或管理员
- 订单操作校验操作者是否为订单相关方
- 被封禁用户在登录时即被拦截，无法获取令牌
- 手机号注册时检查唯一性，编辑时检查冲突

#### 3.5.3 可维护性

- 采用前后端分离架构，前端和后端可独立开发、部署和维护
- 后端代码按 MVC 模式组织：models（数据模型）、routes（路由/控制器）、middleware（中间件）、config（配置）、utils（工具函数）
- 前端小程序代码按页面组织，每页包含 .js/.wxml/.wxss/.json 四个文件
- API 调用统一封装在 api/index.js 中，前端页面通过统一接口调用
- 数据库模型使用 Sequelize ORM 定义，便于表结构变更和迁移

#### 3.5.4 可移植性

- 后端基于 Node.js 运行时，支持跨平台运行（Windows、macOS、Linux）
- 数据库连接信息通过环境变量配置，不硬编码在代码中
- 小程序端请求地址通过配置文件管理，切换部署环境时仅需修改配置

#### 3.5.5 易用性

- 小程序提供底部 Tab 栏导航，核心功能（首页、商品、发布、我的）一键直达
- 首页提供分类快捷入口和热门/最新商品推荐
- 商品列表支持关键词搜索、分类筛选、成色筛选、价格区间筛选、排序等多种检索方式
- 订单按买家/卖家双视角分别展示，状态清晰可辨
- 聊天功能以会话列表形式组织，方便买卖双方就特定商品沟通

### 3.6 数据需求

#### 数据库表结构

**users 表（用户表）：**

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | 主键，自增 | 用户唯一标识 |
| nickname | VARCHAR(50) | 非空 | 用户昵称 |
| phone | VARCHAR(11) | 非空，唯一 | 手机号（兼登录账号） |
| password | VARCHAR(255) | 非空 | 密码哈希值 |
| avatar | VARCHAR(500) | 可空 | 头像 URL |
| role | ENUM('buyer','seller','admin') | 非空，默认'buyer' | 用户角色 |
| student_id | VARCHAR(30) | 可空 | 学号 |
| college | VARCHAR(100) | 可空 | 所属学院 |
| wx_openid | VARCHAR(100) | 可空，唯一 | 微信 OpenID |
| is_banned | BOOLEAN | 非空，默认 false | 是否被封禁 |
| created_at | DATETIME | 自动 | 创建时间 |
| updated_at | DATETIME | 自动 | 更新时间 |

**goods 表（商品表）：**

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | 主键，自增 | 商品唯一标识 |
| seller_id | INTEGER | 非空 | 卖家用户 ID |
| title | VARCHAR(100) | 非空 | 商品标题 |
| description | TEXT | 非空 | 商品描述 |
| price | DECIMAL(10,2) | 非空 | 商品价格 |
| category_id | TINYINT | 非空 | 分类编号（1-8） |
| condition | TINYINT | 非空 | 成色编号（1-5） |
| images | JSON | 非空，默认[] | 商品图片 URL 数组 |
| status | ENUM('available','sold','offline') | 非空，默认'available' | 商品状态 |
| review_status | ENUM('pending','approved','rejected') | 非空，默认'pending' | 审核状态 |
| rejection_reason | VARCHAR(255) | 可空 | 拒绝原因 |
| view_count | INTEGER | 非空，默认0 | 浏览次数 |
| created_at | DATETIME | 自动 | 创建时间 |
| updated_at | DATETIME | 自动 | 更新时间 |

**orders 表（订单表）：**

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | 主键，自增 | 订单唯一标识 |
| goods_id | INTEGER | 非空 | 关联商品 ID |
| buyer_id | INTEGER | 非空 | 买家用户 ID |
| seller_id | INTEGER | 非空 | 卖家用户 ID |
| status | ENUM('pending','confirmed','completed','cancelled') | 非空，默认'pending' | 订单状态 |
| cancel_reason | VARCHAR(255) | 可空 | 取消原因 |
| created_at | DATETIME | 自动 | 创建时间 |
| updated_at | DATETIME | 自动 | 更新时间 |

**favorites 表（收藏表）：**

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | 主键，自增 | 收藏记录唯一标识 |
| user_id | INTEGER | 非空 | 用户 ID |
| goods_id | INTEGER | 非空 | 商品 ID |
| created_at | DATETIME | 自动 | 创建时间 |
| — | — | UNIQUE(user_id, goods_id) | 联合唯一索引 |

**messages 表（消息表）：**

| 字段名 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | INTEGER | 主键，自增 | 消息唯一标识 |
| sender_id | INTEGER | 非空 | 发送者用户 ID |
| receiver_id | INTEGER | 非空 | 接收者用户 ID |
| goods_id | INTEGER | 非空 | 关联商品 ID |
| content | TEXT | 非空 | 消息内容 |
| is_read | BOOLEAN | 非空，默认 false | 是否已读 |
| created_at | DATETIME | 自动 | 创建时间 |

#### 数据分类定义

**商品分类（category_id）：**

| ID | 分类名称 |
|---|---|
| 1 | 数码电子 |
| 2 | 图书教材 |
| 3 | 生活用品 |
| 4 | 服装鞋帽 |
| 5 | 运动户外 |
| 6 | 美妆护肤 |
| 7 | 食品零食 |
| 8 | 其他 |

**商品成色（condition）：**

| ID | 成色描述 |
|---|---|
| 1 | 全新 |
| 2 | 几乎全新 |
| 3 | 轻微使用痕迹 |
| 4 | 明显使用痕迹 |
| 5 | 破损但可用 |

### 3.7 其他需求

**日志需求：** 当前版本未做专门日志记录。建议后续版本接入日志框架（如 winston），记录关键操作日志。

**国际化需求：** 本系统面向中文校园用户，所有界面文字和提示信息使用中文，无需国际化支持。

---

## 4 数据字典

| 数据项 | 类型 | 长度/范围 | 格式要求 | 说明 |
|---|---|---|---|---|
| 手机号 | 字符串 | 11位 | 纯数字，1开头 | 用户登录标识 |
| 密码 | 字符串 | 不限 | 存储时为 bcrypt 哈希值 | 登录凭证 |
| 用户昵称 | 字符串 | 1-50字符 | — | 用户显示名称 |
| 学号 | 字符串 | 1-30字符 | — | 学校学号 |
| 学院名称 | 字符串 | 1-100字符 | — | 所属学院 |
| 商品标题 | 字符串 | 1-100字符 | — | 商品名称 |
| 商品描述 | 文本 | 不限 | — | 商品详细描述 |
| 商品价格 | 十进制 | 整数部分≤8位，小数2位 | ≥0 | 商品售价（元） |
| 图片URL数组 | JSON数组 | 最多6项 | URL字符串数组 | 商品图片链接 |
| 浏览次数 | 整数 | ≥0 | — | 商品被查看次数 |
| 拒绝原因 | 字符串 | 0-255字符 | — | 审核拒绝说明 |
| 取消原因 | 字符串 | 0-255字符 | — | 订单取消说明 |
| 消息内容 | 文本 | 不限 | — | 聊天消息文字 |
| JWT令牌 | 字符串 | — | 标准 JWT 格式 | 身份认证令牌，有效期7天 |

---

## 5 验证需求

| 验证编号 | 需求标识 | 验证内容 | 验证方法 |
|---|---|---|---|
| VR-01 | FR-01-001 | 手机号唯一性校验 | 注册已存在手机号，验证返回 400 错误 |
| VR-02 | FR-01-002 | 密码校验 | 使用错误密码登录，验证返回 401 |
| VR-03 | FR-01-002 | 封禁用户拦截 | 使用封禁账号登录，验证返回 403 |
| VR-04 | FR-03-003 | 角色权限校验 | 以 buyer 角色发布商品，验证返回 403 |
| VR-05 | FR-03-004 | 所有者校验 | 非商品卖家编辑商品，验证返回 403 |
| VR-06 | FR-04-001 | 自购校验 | 购买自己发布的商品，验证返回 403 |
| VR-07 | FR-04-001 | 重复下单校验 | 对已有进行中订单的商品下单，验证返回 400 |
| VR-08 | FR-04-005 | 状态流转校验 | 对非 pending 订单执行确认，验证返回 400 |
| VR-09 | FR-04-007 | 状态流转校验 | 对非 confirmed 订单执行确认收货，验证返回 400 |
| VR-10 | FR-07-002 | 管理员保护 | 尝试封禁管理员账号，验证返回 400 |
| VR-11 | FR-07-004 | 审核功能 | 审核通过商品后验证商品出现在列表中 |
| VR-12 | FR-03-001 | 分页功能 | 指定 page 和 pageSize 参数，验证返回正确的分页数据 |
| VR-13 | FR-03-001 | 多条件筛选 | 组合关键词、分类、价格区间筛选，验证返回结果正确 |
| VR-14 | FR-06-003 | 消息发送 | 发送消息后接收方会话列表中出现新消息 |
| VR-15 | M01-M08 | API 响应格式 | 验证所有接口返回格式为 {code, message, data} |

---

## 6 附录

### 附录 A：订单状态流转图

```
                    ┌──────────┐
                    │  pending  │ ← 初始状态（买家下单）
                    │  (待确认)  │
                    └────┬─────┘
                         │
            ┌────────────┼────────────┐
            │            │            │
            ▼            ▼            ▼
    ┌──────────┐  ┌──────────┐       卖家确认
    │ cancelled │  │ confirmed │ ←─────────────
    │  (已取消)  │  │  (已确认)  │
    └──────────┘  └────┬─────┘
                       │
                       │ 买家确认收货
                       ▼
                 ┌──────────┐
                 │ completed│
                 │  (已完成) │
                 └──────────┘
```

### 附录 B：商品审核状态流转图

```
    ┌──────────┐     管理员通过      ┌──────────┐
    │  pending  │ ──────────────────▶ │ approved  │
    │  (待审核)  │                    │  (已通过)  │
    └──────────┘                    └──────────┘
         │
         │ 管理员拒绝
         ▼
    ┌──────────┐
    │ rejected │
    │  (已拒绝) │
    └──────────┘
```

### 附录 C：商品分类与成色枚举值

| 枚举类型 | 值 | 含义 |
|---|---|---|
| category_id | 1 | 数码电子 |
| category_id | 2 | 图书教材 |
| category_id | 3 | 生活用品 |
| category_id | 4 | 服装鞋帽 |
| category_id | 5 | 运动户外 |
| category_id | 6 | 美妆护肤 |
| category_id | 7 | 食品零食 |
| category_id | 8 | 其他 |
| condition | 1 | 全新 |
| condition | 2 | 几乎全新 |
| condition | 3 | 轻微使用痕迹 |
| condition | 4 | 明显使用痕迹 |
| condition | 5 | 破损但可用 |

### 附录 D：错误码定义

| HTTP 状态码 | 含义 | 典型场景 |
|---|---|---|
| 400 | 请求参数错误 | 参数缺失、格式错误、业务校验失败 |
| 401 | 未授权 | 密码错误、令牌无效 |
| 403 | 禁止访问 | 权限不足、账号被封禁 |
| 404 | 资源不存在 | 用户不存在、商品不存在、订单不存在 |
| 500 | 服务器内部错误 | 服务端未捕获异常 |

---
